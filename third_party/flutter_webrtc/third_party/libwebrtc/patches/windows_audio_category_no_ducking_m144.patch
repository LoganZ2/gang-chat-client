diff --git a/modules/audio_device/win/audio_device_core_win.cc b/modules/audio_device/win/audio_device_core_win.cc
index f21f7aae6f..78cd0bab4c 100644
--- a/modules/audio_device/win/audio_device_core_win.cc
+++ b/modules/audio_device/win/audio_device_core_win.cc
@@ -77,6 +77,27 @@ enum { COM_THREADING_MODEL = COINIT_MULTITHREADED };
 
 enum { kAecCaptureStreamIndex = 0, kAecRenderStreamIndex = 1 };
 
+HRESULT SetAudioClientCategory(IAudioClient* client,
+                               AUDIO_STREAM_CATEGORY category) {
+  if (client == nullptr) {
+    return E_POINTER;
+  }
+
+  IAudioClient2* client2 = nullptr;
+  HRESULT hr = client->QueryInterface(__uuidof(IAudioClient2),
+                                      reinterpret_cast<void**>(&client2));
+  if (FAILED(hr) || client2 == nullptr) {
+    return hr;
+  }
+
+  AudioClientProperties properties = {};
+  properties.cbSize = sizeof(properties);
+  properties.eCategory = category;
+  hr = client2->SetClientProperties(&properties);
+  SAFE_RELEASE(client2);
+  return hr;
+}
+
 // An implementation of IMediaBuffer, as required for
 // IMediaObject::ProcessOutput(). After consuming data provided by
 // ProcessOutput(), call SetLength() to update the buffer availability.
@@ -381,8 +402,8 @@ AudioDeviceWindowsCore::AudioDeviceWindowsCore()
       _microphoneIsInitialized(false),
       _usingInputDeviceIndex(false),
       _usingOutputDeviceIndex(false),
-      _inputDevice(AudioDeviceModule::kDefaultCommunicationDevice),
-      _outputDevice(AudioDeviceModule::kDefaultCommunicationDevice),
+      _inputDevice(AudioDeviceModule::kDefaultDevice),
+      _outputDevice(AudioDeviceModule::kDefaultDevice),
       _inputDeviceIndex(0),
       _outputDeviceIndex(0) {
   RTC_DLOG(LS_INFO) << __FUNCTION__ << " created";
@@ -671,10 +692,7 @@ int32_t AudioDeviceWindowsCore::InitSpeakerLocked() {
     // Refresh the selected rendering endpoint device using current index
     ret = _GetListDevice(eRender, _outputDeviceIndex, &_ptrDeviceOut);
   } else {
-    ERole role;
-    (_outputDevice == AudioDeviceModule::kDefaultDevice)
-        ? role = eConsole
-        : role = eCommunications;
+    ERole role = eConsole;
     // Refresh the selected rendering endpoint device using role
     ret = _GetDefaultDevice(eRender, role, &_ptrDeviceOut);
   }
@@ -743,10 +761,7 @@ int32_t AudioDeviceWindowsCore::InitMicrophoneLocked() {
     // Refresh the selected capture endpoint device using current index
     ret = _GetListDevice(eCapture, _inputDeviceIndex, &_ptrDeviceIn);
   } else {
-    ERole role;
-    (_inputDevice == AudioDeviceModule::kDefaultDevice)
-        ? role = eConsole
-        : role = eCommunications;
+    ERole role = eConsole;
     // Refresh the selected capture endpoint device using role
     ret = _GetDefaultDevice(eCapture, role, &_ptrDeviceIn);
   }
@@ -1447,12 +1462,12 @@ int32_t AudioDeviceWindowsCore::SetPlayoutDevice(
     return -1;
   }
 
-  ERole role(eCommunications);
+  ERole role(eConsole);
 
   if (device == AudioDeviceModule::kDefaultDevice) {
     role = eConsole;
   } else if (device == AudioDeviceModule::kDefaultCommunicationDevice) {
-    role = eCommunications;
+    role = eConsole;
   }
 
   MutexLock lock(&mutex_);
@@ -1713,12 +1728,12 @@ int32_t AudioDeviceWindowsCore::SetRecordingDevice(
     return -1;
   }
 
-  ERole role(eCommunications);
+  ERole role(eConsole);
 
   if (device == AudioDeviceModule::kDefaultDevice) {
     role = eConsole;
   } else if (device == AudioDeviceModule::kDefaultCommunicationDevice) {
-    role = eCommunications;
+    role = eConsole;
   }
 
   MutexLock lock(&mutex_);
@@ -1841,6 +1856,11 @@ int32_t AudioDeviceWindowsCore::InitPlayout() {
                                (void**)&_ptrClientOut);
   EXIT_ON_ERROR(hr);
 
+  hr = SetAudioClientCategory(_ptrClientOut, AudioCategory_Media);
+  if (FAILED(hr)) {
+    RTC_LOG(LS_WARNING) << "Set render audio category failed: " << hr;
+  }
+
   // Retrieve the stream format that the audio engine uses for its internal
   // processing (mixing) of shared-mode streams.
   hr = _ptrClientOut->GetMixFormat(&pWfxOut);
@@ -2156,6 +2176,11 @@ int32_t AudioDeviceWindowsCore::InitRecording() {
                               (void**)&_ptrClientIn);
   EXIT_ON_ERROR(hr);
 
+  hr = SetAudioClientCategory(_ptrClientIn, AudioCategory_Other);
+  if (FAILED(hr)) {
+    RTC_LOG(LS_WARNING) << "Set capture audio category failed: " << hr;
+  }
+
   // Retrieve the stream format that the audio engine uses for its internal
   // processing (mixing) of shared-mode streams.
   hr = _ptrClientIn->GetMixFormat(&pWfxIn);
@@ -3441,7 +3466,7 @@ int AudioDeviceWindowsCore::SetDMOProperties() {
   int inDevIndex = _inputDeviceIndex;
   int outDevIndex = _outputDeviceIndex;
   if (!_usingInputDeviceIndex) {
-    ERole role = eCommunications;
+    ERole role = eConsole;
     if (_inputDevice == AudioDeviceModule::kDefaultDevice) {
       role = eConsole;
     }
@@ -3452,7 +3477,7 @@ int AudioDeviceWindowsCore::SetDMOProperties() {
   }
 
   if (!_usingOutputDeviceIndex) {
-    ERole role = eCommunications;
+    ERole role = eConsole;
     if (_outputDevice == AudioDeviceModule::kDefaultDevice) {
       role = eConsole;
     }
diff --git a/modules/audio_device/win/core_audio_base_win.cc b/modules/audio_device/win/core_audio_base_win.cc
index be90bb466d..ba58635b4c 100644
--- a/modules/audio_device/win/core_audio_base_win.cc
+++ b/modules/audio_device/win/core_audio_base_win.cc
@@ -389,7 +389,7 @@ bool CoreAudioBase::Init() {
   // these types of settings belongs to the client and not the utility parts.
   if (core_audio_utility::GetAudioClientVersion() >= 2) {
     if (FAILED(core_audio_utility::SetClientProperties(
-            static_cast<IAudioClient2*>(audio_client.Get())))) {
+            static_cast<IAudioClient2*>(audio_client.Get()), GetDataFlow()))) {
       return false;
     }
   }
diff --git a/modules/audio_device/win/core_audio_utility_win.cc b/modules/audio_device/win/core_audio_utility_win.cc
index dbfefa0428..7196f60282 100644
--- a/modules/audio_device/win/core_audio_utility_win.cc
+++ b/modules/audio_device/win/core_audio_utility_win.cc
@@ -36,6 +36,18 @@ namespace {
 
 using core_audio_utility::ErrorToString;
 
+AUDIO_STREAM_CATEGORY AudioCategoryForDataFlow(EDataFlow data_flow) {
+  switch (data_flow) {
+    case eRender:
+      return AudioCategory_Media;
+    case eCapture:
+      return AudioCategory_Other;
+    case eAll:
+    default:
+      return AudioCategory_Other;
+  }
+}
+
 // Converts from channel mask to list of included channels.
 // Each audio data format contains channels for one or more of the positions
 // listed below. The number of channels simply equals the number of nonzero
@@ -895,7 +907,7 @@ ComPtr<IAudioClient3> CreateClient3(absl::string_view device_id,
   return CreateClient3Internal(device.Get());
 }
 
-HRESULT SetClientProperties(IAudioClient2* client) {
+HRESULT SetClientProperties(IAudioClient2* client, EDataFlow data_flow) {
   RTC_DLOG(LS_INFO) << "SetClientProperties";
   RTC_DCHECK(client);
   if (GetAudioClientVersion() < 2) {
@@ -904,9 +916,8 @@ HRESULT SetClientProperties(IAudioClient2* client) {
   }
   AudioClientProperties props = {0};
   props.cbSize = sizeof(AudioClientProperties);
-  // Real-time VoIP communication.
-  // TODO(henrika): other categories?
-  props.eCategory = AudioCategory_Communications;
+  props.eCategory = AudioCategoryForDataFlow(data_flow);
+  RTC_DLOG(LS_INFO) << "category: " << static_cast<int>(props.eCategory);
   // Hardware-offloaded audio processing allows the main audio processing tasks
   // to be performed outside the computer's main CPU. Check support and log the
   // result but hard-code `bIsOffload` to FALSE for now.
diff --git a/modules/audio_device/win/core_audio_utility_win.h b/modules/audio_device/win/core_audio_utility_win.h
index bbf5710c9c..4ed2863383 100644
--- a/modules/audio_device/win/core_audio_utility_win.h
+++ b/modules/audio_device/win/core_audio_utility_win.h
@@ -390,12 +390,12 @@ Microsoft::WRL::ComPtr<IAudioClient3> CreateClient3(absl::string_view device_id,
                                                     EDataFlow data_flow,
                                                     ERole role);
 
-// Sets the AudioCategory_Communications category. Should be called before
-// GetSharedModeMixFormat() and IsFormatSupported(). The `client` argument must
-// be an IAudioClient2 or IAudioClient3 interface pointer, hence only supported
-// on Windows 8 and above.
-// TODO(henrika): evaluate effect (if any).
-HRESULT SetClientProperties(IAudioClient2* client);
+// Sets a Windows audio category matching the stream direction. Should be
+// called before GetSharedModeMixFormat() and IsFormatSupported(). The `client`
+// argument must be an IAudioClient2 or IAudioClient3 interface pointer, hence
+// only supported on Windows 8 and above. Returns AUDCLNT_E_UNSUPPORTED_FORMAT
+// if `GetAudioClientVersion()` < 2.
+HRESULT SetClientProperties(IAudioClient2* client, EDataFlow data_flow);
 
 // Returns the buffer size limits of the hardware audio engine in
 // 100-nanosecond units given a specified `format`. Does not require prior
