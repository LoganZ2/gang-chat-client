name: release

# 流程：prepare（建 draft release）→ build-macos & build-windows（并行构建，直接上传产物）→ release（发布）
# 全程不依赖任何外部 action，仅用 git / curl / python3 原生工具。
# 触发方式：push v* tag，或 Actions 页面手动触发。

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: '版本号（不带 v 前缀，例如 1.2.0）'
        required: true

jobs:
  prepare:
    runs-on: macos-arm64
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      release_id: ${{ steps.release.outputs.release_id }}
    steps:
      - name: Resolve version
        id: version
        run: |
          if [ "${GITHUB_EVENT_NAME}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION="${GITHUB_REF_NAME#v}"
          fi
          if [ -z "$VERSION" ]; then echo "version is empty"; exit 1; fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"
          echo "Resolved version: $VERSION"

      - name: Create draft release
        id: release
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          API="${GITHUB_SERVER_URL}/api/v1/repos/${GITHUB_REPOSITORY}"
          TAG="${{ steps.version.outputs.tag }}"

          # 不用 -f，让 409 时也能拿到响应体
          RESP="$(curl -sS -X POST "$API/releases" \
            -H "Authorization: token $TOKEN" \
            -H "Content-Type: application/json" \
            -d "{\"tag_name\":\"$TAG\",\"target_commitish\":\"${GITHUB_SHA}\",\"name\":\"$TAG\",\"draft\":true,\"prerelease\":false}")"
          RELEASE_ID="$(printf '%s' "$RESP" | python3 -c 'import sys,json; print(json.load(sys.stdin).get("id",""))' 2>/dev/null)"

          # 409（tag/release 已存在）时按 tag 查现有 release
          if [ -z "$RELEASE_ID" ]; then
            echo "Create returned no id (possibly 409), fetching existing release by tag..."
            RESP="$(curl -fsS "$API/releases/tags/$TAG" -H "Authorization: token $TOKEN")"
            RELEASE_ID="$(printf '%s' "$RESP" | python3 -c 'import sys,json; print(json.load(sys.stdin)["id"])')"
          fi

          if [ -z "$RELEASE_ID" ]; then echo "Failed to get release id: $RESP"; exit 1; fi
          echo "release_id=$RELEASE_ID" >> "$GITHUB_OUTPUT"
          echo "Release id=$RELEASE_ID"

  build-macos:
    runs-on: macos-arm64
    needs: [prepare]
    steps:
      - name: Checkout
        uses: https://gitea.com/actions/checkout@v4

      - name: Flutter sanity check
        run: |
          command -v flutter >/dev/null 2>&1 || { echo "flutter not found in PATH"; exit 1; }
          flutter --version

      - name: Get dependencies
        run: flutter pub get

      - name: Build macOS (release)
        run: flutter build macos --release --build-name="${{ needs.prepare.outputs.version }}"

      - name: Package DMG
        id: dmg
        run: |
          APP="build/macos/Build/Products/Release/client.app"
          [ -d "$APP" ] || { echo "app bundle not found at $APP"; exit 1; }
          DMG="GangChat-${{ needs.prepare.outputs.version }}-macos.dmg"
          STAGING="$(mktemp -d)"
          cp -R "$APP" "$STAGING/"
          ln -s /Applications "$STAGING/Applications"
          hdiutil create -volname "Gang Chat" -srcfolder "$STAGING" \
            -ov -format UDZO "$DMG"
          rm -rf "$STAGING"
          echo "name=$DMG" >> "$GITHUB_OUTPUT"
          ls -lh "$DMG"

      - name: Upload DMG to release
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          DMG="${{ steps.dmg.outputs.name }}"
          API="${GITHUB_SERVER_URL}/api/v1/repos/${GITHUB_REPOSITORY}"
          RELEASE_ID="${{ needs.prepare.outputs.release_id }}"
          curl -fsS -X POST "$API/releases/$RELEASE_ID/assets?name=$DMG" \
            -H "Authorization: token $TOKEN" \
            -F "attachment=@$DMG"
          echo "Uploaded $DMG"

  build-windows:
    runs-on: windows
    needs: [prepare]
    env:
      PUB_HOSTED_URL: https://pub.flutter-io.cn
      FLUTTER_STORAGE_BASE_URL: https://storage.flutter-io.cn
    steps:
      - name: Checkout
        uses: https://gitea.com/actions/checkout@v4

      - name: Flutter sanity check
        shell: powershell
        run: |
          if (-not (Get-Command flutter -ErrorAction SilentlyContinue)) {
            Write-Error "flutter not found in PATH"; exit 1
          }
          flutter --version

      - name: Get dependencies
        run: flutter pub get

      - name: Build Windows (release)
        run: flutter build windows --release --build-name="${{ needs.prepare.outputs.version }}"

      - name: Package ZIP
        id: zip
        shell: powershell
        run: |
          $VERSION = "${{ needs.prepare.outputs.version }}"
          $SRC = "build\windows\x64\runner\Release"
          if (-not (Test-Path $SRC)) { Write-Error "build output not found at $SRC"; exit 1 }
          $ZIP = "GangChat-$VERSION-windows.zip"
          Compress-Archive -Path "$SRC\*" -DestinationPath $ZIP
          # 用 .NET 方法写入，避免 PS5 Out-File -Encoding utf8 带 BOM 导致 runner 解析失败
          [IO.File]::AppendAllText($env:GITHUB_OUTPUT, "name=$ZIP`n")
          Get-Item $ZIP | Select-Object Name, @{n='Size';e={'{0:N0} bytes' -f $_.Length}}

      - name: Upload ZIP to release
        shell: powershell
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $ZIP = "${{ steps.zip.outputs.name }}"
          $API = "$env:GITHUB_SERVER_URL/api/v1/repos/$env:GITHUB_REPOSITORY"
          $RELEASE_ID = "${{ needs.prepare.outputs.release_id }}"
          # 用数组传参避免 PS 多行 backtick 续行解析问题
          $curlArgs = @('-fsS', '-X', 'POST',
            "$API/releases/$RELEASE_ID/assets?name=$ZIP",
            '-H', "Authorization: token $env:TOKEN",
            '-F', "attachment=@$ZIP")
          curl.exe @curlArgs
          Write-Host "Uploaded $ZIP"

  release:
    runs-on: macos-arm64
    needs: [prepare, build-macos, build-windows]
    steps:
      - name: Publish release
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_ID: ${{ needs.prepare.outputs.release_id }}
        run: |
          API="${GITHUB_SERVER_URL}/api/v1/repos/${GITHUB_REPOSITORY}"
          curl -fsS -X PATCH "$API/releases/$RELEASE_ID" \
            -H "Authorization: token $TOKEN" \
            -H "Content-Type: application/json" \
            -d '{"draft":false}'
          echo "Published: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/releases"
